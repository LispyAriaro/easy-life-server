module Constants.TableNames (
  users, userRoles,
  services, serviceImages, serviceProviderCompanies,
  locations, serviceRatings
) where

users = "users"
userRoles = "user_roles"

services = "services"
serviceCategories = "service_categories"
serviceImages = "service_images"

serviceProviderCompanies = "service_provider_companies"

locations = "locations"

serviceRatings = "consumer_service_ratings"
